import { Size } from '@ohos/hypium'

export const paddingTitle: Padding = {
  top: 20,
  left: 20,
  right: 20,
  bottom: 20
}

export const imageTitleSize: SizeOptions = {
  width: 30,
  height: 30
}